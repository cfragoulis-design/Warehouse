[tool.ruff]
target-version = "py312"
exclude = ["app/patches"]

[tool.ruff.lint]
# Enforce import, syntax and Pyflakes correctness without mixing a visual
# restyle into this security checkpoint.
select = ["E4", "E7", "E9", "F"]
