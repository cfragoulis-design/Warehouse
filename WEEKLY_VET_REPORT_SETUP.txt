Weekly Vet Report email via Railway Cron
----------------------------------------

1) Set env vars in Railway:
   You can use the existing variables already configured for production reports:
   - SMTP_HOST
   - SMTP_PORT
   - SMTP_USER
   - SMTP_PASS
   - PRODUCTION_REPORT_TO       (vet email address, currently info@kentarxos.gr)
   - PRODUCTION_REPORT_CC       (optional, currently info@sklavounosmeat.gr)
   - PRODUCTION_REPORT_FROM     (sender, currently info@sklavounosmeat.gr)
   - PRODUCTION_REPORT_TOKEN    (only needed for the optional HTTP trigger)

   Optional overrides, only if you want the weekly vet report to differ from
   the existing production report settings:
   - VET_REPORT_TO
   - VET_REPORT_CC
   - VET_REPORT_FROM
   - WEEKLY_VET_REPORT_TOKEN
   - WEEKLY_VET_REPORT_HOUR     (defaults to 8 Greece time)

   Notes:
   - If VET_REPORT_TO is missing, the app falls back to PRODUCTION_REPORT_TO
     and then to the existing default.
   - If WEEKLY_VET_REPORT_TOKEN is missing, the cron endpoint accepts
     PRODUCTION_REPORT_TOKEN.
   - SMTP_PORT=587 is supported with STARTTLS, which matches Brevo.

2) Automatic schedule (included in the web app):
   The web service checks every 5 minutes and sends once on Monday during
   WEEKLY_VET_REPORT_HOUR (08:00 Greece time by default). The report_runs
   table prevents duplicate emails, including when Railway runs more than
   one web instance or restarts the service.

   No separate Railway Cron Service is required.

3) Optional Railway cron service as a backup:
   - Start command:
     python -m app.weekly_vet_report_cron

   - Cron schedules:
     0 5 * * 1
     0 6 * * 1

   Railway cron schedules run in UTC. Greece is UTC+3 during daylight saving
   time and UTC+2 during winter time. Keep both schedules enabled: the cron
   worker checks the Athens hour and only sends when it is exactly 08:00.
   The report_runs table also prevents duplicate weekly emails.

   Alternative HTTP trigger, if you use an external HTTP cron:
   - /internal/weekly-vet-report-cron?token=<WEEKLY_VET_REPORT_TOKEN>

4) Admin preview:
   - /admin/vet-report/weekly/

5) Manual send from the admin dashboard:
   - Send Weekly Report

The cron endpoint sends the last completed Monday-Saturday period and uses
the report_runs table so Railway retries do not send the same weekly report twice.
